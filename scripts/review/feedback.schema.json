{
  "type": "object",
  "additionalProperties": false,
  "required": ["plusses", "deltas"],
  "$defs": {
    "door": {"type": "string", "enum": ["structure", "presentation", "dynamic interaction", "design", "tests"]},
    "severity": {"type": "string", "enum": ["violation", "concern", "note"]}
  },
  "properties": {
    "plusses": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["door", "file", "line", "happened", "why", "principle"],
        "properties": {
          "door": {"$ref": "#/$defs/door"},
          "file": {"type": "string"},
          "line": {"type": "integer", "minimum": 1},
          "happened": {"type": "string"},
          "why": {"type": "string"},
          "checked": {"type": "string"},
          "principle": {"type": "string"}
        }
      }
    },
    "deltas": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["door", "severity", "file", "line", "happened", "why", "change", "principle"],
        "properties": {
          "door": {"$ref": "#/$defs/door"},
          "severity": {"$ref": "#/$defs/severity"},
          "file": {"type": "string"},
          "line": {"type": "integer", "minimum": 1},
          "happened": {"type": "string"},
          "why": {"type": "string"},
          "change": {"type": "string"},
          "checked": {"type": "string"},
          "principle": {"type": "string"}
        }
      }
    }
  }
}
